# Public, browser-side build config for production builds (e.g. Vercel).
# Only VITE_* values belong here: they are inlined into the JS bundle.
# The Meta Pixel id is public by design; secrets stay in the host's env settings.
VITE_META_PIXEL_ID=2296234397888826
